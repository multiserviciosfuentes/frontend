export default class Contact {
  constructor(fullName = null, phone = null, email = null, address = null) {
    this.fullName = fullName
    this.phone = phone
    this.email = email
    this.address = address
  }
}
